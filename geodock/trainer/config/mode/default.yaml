# @package _global_

# default running mode

default_mode: True

hydra:
  # default output paths for all file logs
  run:
    dir: GeoDock/logs/runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: Geodock/logs/multiruns/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
